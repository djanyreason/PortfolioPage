import { createTheme } from '@mui/material';

const PortfolioTheme = createTheme({
  palette: {
    primary: {
      main: '#37474F',
    },
    secondary: {
      main: '#4E8A84',
    },
    success: {
      main: '#ffc107',
    },
  },
});

export default PortfolioTheme;
